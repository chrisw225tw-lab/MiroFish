# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= 核心框架 =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM 相关 =============
# OpenAI SDK（统一使用 OpenAI 格式调用 LLM）
openai>=1.0.0

# ============= Zep Cloud =============
zep-cloud==3.25.0
httpx>=0.27.0

# ============= 自建 Graphiti =============
# 本地知识图谱引擎（替代 Zep Cloud；版本需与 graphiti-mcp 容器内一致）
graphiti-core[falkordb]==0.28.2
# falkordb 1.6.2 会向 redis-py 传 himport_registry，与 redis 8.x 不兼容；
# 锁到 graphiti-mcp 容器同款组合
falkordb==1.6.0
redis>=7.3.0,<8
# neo4j driver：camel-oasis 声明 ==5.23.0，graphiti-core 需要 >=5.26.0；
# driver 小版本向后兼容，此处显式使用 5.26+（pip 会提示冲突警告，可忽略）
neo4j>=5.26.0

# ============= OASIS 社交媒体模拟 =============
# OASIS 社交模拟框架
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= 文件处理 =============
PyMuPDF>=1.24.0
# 编码检测（支持非UTF-8编码的文本文件）
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= 工具库 =============
# 环境变量加载
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0
